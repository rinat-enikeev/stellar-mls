Onym is an encrypted group chat app. To test, create a group and send messages. The app generates a cryptographic identity on first launch.

Safety features: Users can block any contact and report groups for objectionable content. All user-generated content (messages, media, voice messages) is only visible to group members.