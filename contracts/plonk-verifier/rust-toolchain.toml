[toolchain]
# Soroban SDK 26 requires rustc ≥ 1.91.0. Pinned per-crate (not at the
# repo root) because the prover side still uses 1.88.0; the per-contract
# bumps to 1.91.0 happen as each is rewritten in Phase C.2.
channel = "1.91.0"
