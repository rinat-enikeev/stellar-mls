\section{Migration Paths}
\label{sec:migration}

% For each starting configuration in §5: enumerate the concrete
% transformations that would yield a PQ-ready endpoint.
%
% Categorize each transformation as:
%   (a) ready today (tooling exists, just engineering work);
%   (b) blocked on host functions (verifier cost prohibitive without);
%   (c) blocked on tooling (no production-grade implementation);
%   (d) blocked on cryptographic open problems (no construction).
%
% Figure 2: TikZ DAG in figures/migration-graph.tex showing reachable
% PQ-ready endpoints from each starting configuration.
%
% Critical citations:
%   - Stellar Protocol 22 ledger entry for BLS12-381 host functions
%     (justifies "Plonky3 path is type-(b) blocked").
%   - Plonky3 README / spec (current readiness level).
%   - STARK-on-Ethereum gas measurements (justifies type-(b) for
%     Eth L1 transparent-setup migration).
%
% Compromise table: for each non-blocked migration, what is given up
% (proof size grows, prover time grows, ceremony complexity, etc.).

TODO: draft after §5 lock.
