\section{Open Problems}
\label{sec:open}

% Gaps in literature and tooling that the taxonomy makes visible.
%
%   - Hash-based recursion at production prover speed: Plonky2/3 close
%     the gap on prover time but not yet at the L1-anchorable proof
%     size end. What's the lower bound on FRI proof size for a fixed
%     security level?
%
%   - Transparent-setup pairings: every pairing-based SNARK in current
%     use requires a setup ceremony. Is there a transparent-setup
%     instantiation that retains pairing-friendly verifier costs?
%
%   - Non-pairing host functions on chains with pairing-only precompiles:
%     Stellar's BLS12-381 host functions don't help FRI verifiers. What
%     would the right host-function ABI look like for a chain that
%     wants to support multiple proof systems?
%
%   - Formal verification of full SNARK pipelines: most existing
%     formal work covers the relation, not the prover-verifier-host
%     stack. Verified compilation from a high-level circuit DSL to a
%     deployed verifier is an open engineering problem.
%
%   - PQ migration without a proof-system change: can a deployed
%     Groth16 system migrate to PQ-secure verification without changing
%     the relation, the ceremony, or the host? Open.
%
%   - Lattice-based SNARKs in deployment: no production system is
%     known to use a lattice SNARK. Maturity gap is the open variable.

TODO: draft after §7 lock.
