[package]
name = "sep-xxxx-relayer"
version = "0.1.0"
edition = "2021"
description = "Fee-decoupling HTTP relayer for SEP-XXXX Soroban contract"
license = "MIT"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tower-http = { version = "0.5", features = ["limit"] }
base64 = "0.22"
hex = "0.4"
